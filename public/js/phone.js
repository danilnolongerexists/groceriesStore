var element = document.getElementById('phone-log');
var maskOptions = {
    mask: '+7 000 000 00 00',
    lazy: false,
    placeholderChar: ' '
}
var mask = new IMask(element, maskOptions);

var element = document.getElementById('phone-reg');
var maskOptions = {
    mask: '+7 000 000 00 00',
    lazy: false,
    placeholderChar: ' '
}
var mask = new IMask(element, maskOptions);

var element = document.getElementById('phone-upd');
var maskOptions = {
    mask: '+7 000 000 00 00',
    lazy: false,
    placeholderChar: ' '
}
var mask = new IMask(element, maskOptions);
